import {MeasurementPlanV3} from "../main";
import {BrowserContext} from "playwright";

export async function makeStealth(plan: MeasurementPlanV3, instance: BrowserContext) {
    /* Our bot evasion mechanisms are not public due to potential for abuse. Please reach out for details. */
}
